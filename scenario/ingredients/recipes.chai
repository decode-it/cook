cook["book"].recipe("foobar", fun(r) {
    r.add("", "*", fun(f) {
        return true
    })
    r.remove("", "main.cpp")
})

root.book("book", fun(book) {
    book.recipe("main", "executable", fun(r){
        r.add("", "main.cpp")
        r.depends_on("foobar", fun(b) {
            return (!(b.flags() & Type.IncludePath))
        })
    })
})
